import { StyledHeading } from "../StyleComponents/Heading";
import { MainWrappper } from "../StyleComponents/Wrappers";

export const Contact = () => {
  return (
    <MainWrappper>
      <StyledHeading>Kan man kontakta någon som inte vem den är?</StyledHeading>
    </MainWrappper>
  );
};
