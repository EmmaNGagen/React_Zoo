import { Link } from "react-router-dom";
import { StyledHeading } from "../StyleComponents/Heading";
import { MainWrappper } from "../StyleComponents/Wrappers";

export const About = () => {
  return (
    <MainWrappper>
      <StyledHeading>
        Vi är under konstruktion. Vi vet inte vilka vi är än!
      </StyledHeading>
    </MainWrappper>
  );
};
